# Core modules (mandatory)
# voici-core 0.10.0 has a broken module-federation match with all published
# jupyterlite-pyodide-kernel wheels (voila-dashboards/voici#178, "No kernel
# available"). Downgrade the whole stack to the 0.6.x line, which voici-core
# 0.9.0 was actually built against.
jupyterlite-core>=0.6.0,<0.7
jupyterlab>=4.4,<4.5
notebook>=7.4,<7.5


# Python kernel (optional)
# 0.6.1's pyodide-kernel-extension requires @jupyterlab/* ^4.4.0 and
# @jupyterlite/server ^0.6.0 — exactly what voici-core 0.9.0 bundles.
jupyterlite-pyodide-kernel>=0.6.1,<0.7

# Teahouse dependencies
anywidget==0.9.13
voici-core==0.9.0
